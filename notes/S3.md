# Clean Code - clases y comentarios

En esta sección hablaremos principalmente sobre clases y conceptos generales para escribir un mejor código.

Es importante tener presente que así como todo, hay excepciones a cada regla, pero esto debería ser un caso aislado y no el estándar. Tratemos de escribir un mejor código y dejar de lado la comodidad de hacer las cosas como se han venido haciendo, especialmente si esas cosas traen muchos problemas.

### Clases
principio responsabilidad unica
- solo una tarea espeficica
- es mucha talacha usando herencia

### objetos como propiedades

interface PersonProps {
        name        : string;
        gender      : Gender;
        bithdate    : Date;
    }

en vez de pasar elem por elem, mandamos objeto con props
es mas faacil, usar las interfaces, que cambiuar tooodo el construct

### Principio de responsabilidad unica
Eliminar extends, no usamos super

    class UserSettings {
        
        public person: Person;
        public user: User;
        public settings: Settings;

Definimos clases unicas, y al final una clase donde almacenemos cada clase unica

### tarea
Del mismo modo, quitamos extends, creamos clase con cada clase espefcifica
    //? Idea para la nueva clase InputElement
    class InputElement {
        public htmlElement: HtmlElement;
        public inputAttributes: InputAttributes;
        public inputEvents: InputEvents;

        constructor(
            id: string,
            value: string,
            placeholder: string,
            type: HtmlType = 'input',
        ) {
            this.htmlElement = new HtmlElement(id, type);
            this.inputAttributes = new InputAttributes(value, placeholder);
            this.inputEvents = new InputEvents();
        }

- asi podemos reafcatorizar una clase enviando los mismos params
- en el constructor incializamos cad aclase especifica

### Estructra de una clase
1. comenzar propiedaes estaticas
2. privadas y luego publicas

3. Metodos
constrcotres estaticos
constructor
metodos estaticos
metodos privados
metodos demas segun importancia
getters y setteres al final

### comentarios
es horrible leer codigo de otros
el codigo no se debe explicar a si mismo

DEBE dejar explicacion de la solucion
- ojo con librerias
debe ser autoexplicativo
DEBE explicar el porque, no el como
- porque la clase, porque cada metodo,

### uniformidad
problemas similares, soluciones similares
createProduct - createUser
updateProduct - updateUser
